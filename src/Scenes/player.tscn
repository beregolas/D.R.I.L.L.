[gd_scene load_steps=5 format=3 uid="uid://blucy12ccai7y"]

[ext_resource type="Texture2D" uid="uid://ckxma15wusbej" path="res://Images/drill.png" id="1_mwhjs"]
[ext_resource type="Texture2D" uid="uid://bh4npiwio0fx4" path="res://Images/drill_2.png" id="2_ylsew"]

[sub_resource type="GDScript" id="GDScript_b33qm"]
script/source = "extends Area2D


@export var speed = 400 #speed pixels/sec
var screen_size


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _pcess(delta):
	var velocity = Vector2.ZERO # The player's movement vector.
	if Input.is_action_pressed(\"move_down\"):
		velocity.y += 1
	if Input.is_action_pressed(\"move_left\"):
		velocity.x -= 1
	if Input.is_action_pressed(\"move_right\"):
		velocity.x += 1
	if (Input.is_action_pressed(\"move_up\")):
		velocity.y -= 1
		
	if velocity.length() > 0:
		velocity = velocity.normalized() * speed
		$AnimatedSprite2D.play()
	else:
		$AnimatedSprite2D.stop()
	
	position += velocity * delta
	#position.x = clamp(position.x, 0, screen_size.x)
	#position.y = clamp(position.y, 0, screen_size.y)
	
	if velocity.x != 0:
		$AnimatedSprite2D.animation = \"drill\"
		$AnimatedSprite2D.flip_v = false
		$AnimatedSprite2D.flip_h = velocity.x < 0

	
"

[sub_resource type="SpriteFrames" id="SpriteFrames_lq46o"]
animations = [{
"frames": [{
"duration": 1.0,
"texture": ExtResource("1_mwhjs")
}, {
"duration": 1.0,
"texture": ExtResource("2_ylsew")
}],
"loop": true,
"name": &"drill",
"speed": 5.0
}]

[node name="Player" type="Area2D"]
position = Vector2(491, 283)
script = SubResource("GDScript_b33qm")
metadata/_edit_group_ = true

[node name="AnimatedSprite2D" type="AnimatedSprite2D" parent="."]
scale = Vector2(0.3, 0.3)
sprite_frames = SubResource("SpriteFrames_lq46o")
animation = &"drill"

[node name="CollisionPolygon2D" type="CollisionPolygon2D" parent="."]
position = Vector2(0, 0.0501709)
scale = Vector2(1, 0.999647)
polygon = PackedVector2Array(-1, 89.9816, -90, -89, 89, -89)
